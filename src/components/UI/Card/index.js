import React from 'react'
import './style.css'; 
import Card from './UI/Card'; 

const Hero =(props) => {
  return (
    <div className='card'>
      {props.children}
    </div>
  )
} 

export default index
